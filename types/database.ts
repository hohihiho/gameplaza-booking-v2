// 이 파일은 나중에 Supabase CLI로 자동 생성됩니다.
// 임시로 빈 타입을 export 합니다.

export type Database = {
  public: {
    Tables: {
      // 테이블 타입들이 여기에 추가됩니다
    }
    Views: {
      // 뷰 타입들이 여기에 추가됩니다
    }
    Functions: {
      // 함수 타입들이 여기에 추가됩니다
    }
    Enums: {
      // Enum 타입들이 여기에 추가됩니다
    }
  }
}